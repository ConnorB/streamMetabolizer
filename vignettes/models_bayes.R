## ----setup, include=FALSE-----------------------------------------------------
knitr::opts_chunk$set(echo = TRUE)

## ----bayes_warning, eval=FALSE------------------------------------------------
# # here's where you'd find fitting messages:
# select(get_params(mm), warnings, errors)
# get_fit(mm)$warnings
# get_fit(mm)$errors
# 
# # and prediction messages
# select(predict_metab(mm), warnings, errors)

