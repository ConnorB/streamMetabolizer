# Define a package environment for storing data specific to a project during an R session

Define a package environment for storing data specific to a project
during an R session

## Usage

``` r
define_pkg_env()
```

## Value

The package environment.
