% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mm_generate_mcmc_file.R
\name{mm_generate_mcmc_file}
\alias{mm_generate_mcmc_file}
\title{Generate the models in inst/models/bayes}
\usage{
mm_generate_mcmc_file(
  type = "bayes",
  pool_K600 = c("none", "normal", "normal_sdzero", "normal_sdfixed", "linear",
    "linear_sdzero", "linear_sdfixed", "binned", "binned_sdzero", "binned_sdfixed"),
  err_obs_iid = c(TRUE, FALSE),
  err_proc_acor = c(FALSE, TRUE),
  err_proc_acor_light = c(FALSE, TRUE),
  err_proc_iid = c(FALSE, TRUE),
  err_proc_GPP = c(FALSE, TRUE),
  ode_method = c("trapezoid", "euler"),
  GPP_fun = c("linlight", "satlight"),
  ER_fun = c("constant"),
  deficit_src = c("DO_mod", "DO_obs"),
  engine = "stan"
)
}
\arguments{
\item{type}{A string specifying the model type:
\itemize{
\item \code{"mle"}: Maximum likelihood estimation; see \code{\link[=metab_mle]{metab_mle()}}.
\item \code{"bayes"}: Bayesian hierarchical modeling; see \code{\link[=metab_bayes]{metab_bayes()}}.
\item \code{"night"}: Nighttime regression; see \code{\link[=metab_night]{metab_night()}}.
\item \code{"Kmodel"}: Regression of daily \code{K600.daily} estimates against
discharge, time, or other predictors; see \code{\link[=metab_Kmodel]{metab_Kmodel()}}.
\item \code{"sim"}: Simulation of \code{DO.obs} data; see \code{\link[=metab_sim]{metab_sim()}}.
}}

\item{pool_K600}{A string specifying how to pool information among days for
more consistent daily K600 estimates. See \emph{K600 pooling} for details:
\itemize{
\item \code{"none"}: Do not pool K600.
\item \code{"normal"}: Use \verb{K₆₀₀ ∼ N(μ, σ)}.
\item \code{"linear"}: Use \verb{K₆₀₀ ∼ N(B[0] + B[1] × Q, σ)}.
\item \code{"binned"}: Use \verb{K₆₀₀ ∼ N(B[Q_bin], σ)}, where
\verb{μ ∼ N(mu_mu, mu_sigma)} and \verb{σ ∼ N(sigma_mu, sigma_sigma)}.
\item \code{"complete"}: Use complete pooling for \code{type = "Kmodel"}, generally
between preceding K estimates and subsequent GPP and ER estimates.
}}

\item{err_obs_iid}{A logical. Should IID observation error be included? If
not, the model will be fit to the differences in successive DO
measurements, rather than to the DO measurements themselves.}

\item{err_proc_acor}{A logical. Should autocorrelated process error (with the
autocorrelation term \code{phi} fitted) be included? For multi-day Bayesian
process-error models, the AR likelihood continues across day boundaries.}

\item{err_proc_acor_light}{A logical. Should the innovation standard
deviation of autocorrelated process error increase linearly with the
fraction of each day's light occurring at that timestep? Only available
when \code{err_proc_acor = TRUE}.}

\item{err_proc_iid}{A logical. Should IID process error be included?}

\item{err_proc_GPP}{A logical. Should IID process error in GPP be included?
This kind of error occurs only during the day and is used to adjust GPP
before passing it into the dDO/dt equation. \code{GPP_inst} is the corrected
GPP, and \code{GPP_inst_partial} contains the pre-adjustment estimates.}

\item{ode_method}{A string specifying the method used to solve the ordinary
differential equation for DO:
\itemize{
\item \code{"euler"} (formerly \code{"Euler"}): Use conditions at the start of each
timestep.
\item \code{"trapezoid"} (formerly \code{"pairmeans"}): Use mean conditions across each
timestep.
\item For \code{type = "mle"}, \code{"rk2"} and methods accepted by \code{\link[deSolve:ode]{deSolve::ode()}}
are also available. Many have not been extensively tested with
streamMetabolizer models.
}}

\item{GPP_fun}{A string specifying how gross primary productivity (GPP)
varies within each day:
\itemize{
\item \code{"linlight"}: \code{GPP(t) = GPP.daily * light(t) / mean.light}.
\code{GPP.daily} is partitioned among timesteps by their fraction of the day's
average light.
\item \code{"satlight"}: \code{GPP(t) = Pmax * tanh(alpha * light(t) / Pmax)}, where
\code{Pmax} is the maximum possible GPP and \code{alpha} describes the initial
increase with light.
\item \code{"satlightq10temp"}: The saturating light model multiplied by
\code{1.036 ^ (temp.water(t) - 20)}.
\item \code{NA}: Do not estimate GPP; applicable only to \code{type = "Kmodel"}.
}}

\item{ER_fun}{A string specifying how ecosystem respiration (ER) varies
within each day:
\itemize{
\item \code{"constant"}: \code{ER(t) = ER.daily}.
\item \code{"q10temp"}: \code{ER(t) = ER20 * 1.045 ^ (temp.water(t) - 20)}, where \code{ER20}
is ER at 20 degrees C.
\item \code{NA}: Do not estimate ER; applicable only to \code{type = "Kmodel"}.
}}

\item{deficit_src}{A string specifying the DO estimate used to compute the DO
deficit:
\itemize{
\item \code{"DO_mod"}: Use \verb{DO.sat(t) − DO_mod(t)}, the difference between the
equilibrium-saturation value and the current best estimate of the true DO
concentration at that time.
\item \code{"DO_obs"}: Use \verb{DO.sat(t) − DO.obs(t)}.
\item \code{"DO_obs_filter"}: Smooth \code{DO.obs} before nighttime regression;
applicable only to \code{type = "night"}.
\item \code{NA}: Do not estimate DO deficit; applicable only to \code{type = "Kmodel"}.
}}

\item{engine}{A string specifying the fitting engine. Valid combinations are
\code{"nlm"} for MLE, \code{"stan"} for Bayesian models, \code{"lm"} for nighttime
regression, \code{"mean"}, \code{"lm"}, or \code{"loess"} for K models, and \code{"rnorm"}
for simulations.}
}
\description{
Generate the models in inst/models/bayes
}
\keyword{internal}
