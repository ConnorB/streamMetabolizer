% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mm_get_timestep.R
\name{mm_get_timestep}
\alias{mm_get_timestep}
\title{Calculate timesteps in days}
\usage{
mm_get_timestep(
  datetimes,
  format = c("mean", "unique", "modal"),
  require_unique = FALSE,
  tol = 60/(24 * 60 * 60)
)
}
\arguments{
\item{datetimes}{A \code{POSIXct} vector from which to calculate timesteps.}

\item{format}{A string specifying the output format. \code{"mean"} always returns
one value; \code{"unique"} may return more than one, depending on the variation
in timesteps and the value of \code{tol}.}

\item{require_unique}{A logical. Should exactly one unique timestep (within
the tolerance \code{tol}) be required?}

\item{tol}{If \code{format = "unique"}, the minimum difference in days for two
timesteps to be considered distinct. For example, \code{1 / (24 * 60 * 60)} is
one second.}
}
\value{
A numeric vector of timesteps in days. Its length depends on
\code{format}.
}
\description{
Calculate timesteps in days
}
\examples{
datetimes <- Sys.time() +
  as.difftime(c(0, 304, 600, 900.2, 1200, 1500, 1800), units = "secs")
mm_get_timestep(datetimes, "unique", tol = 1 / (24 * 60 * 60))
mm_get_timestep(datetimes, "unique", tol = 5 / (24 * 60 * 60))
mm_get_timestep(datetimes, "mean")
mm_get_timestep(
  datetimes,
  "mean",
  require_unique = TRUE,
  tol = 300 / (24 * 60 * 60)
)

datetimes <- Sys.time() +
  as.difftime(c(-1, 0, 2, 4, 5, 6, 8, 10), units = "days")
mm_get_timestep(datetimes, "modal")
mm_get_timestep(c(), "mean")

try(mm_get_timestep(datetimes, "mean", require_unique = TRUE))
try(mm_get_timestep(c(), "unique", require_unique = TRUE))
}
