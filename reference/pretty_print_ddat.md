# Format and print a summary of data frame of daily values

Compress error and warning messages into one column and only print the
first few rows of data

## Usage

``` r
pretty_print_ddat(ddat, msg.col)
```
