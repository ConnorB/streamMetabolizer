# Locate the persistent compilation cache for an RStan model

Locate the persistent compilation cache for an RStan model

## Usage

``` r
rstan_cache_file(model_path)
```

## Arguments

- model_path:

  Path to a Stan program.

## Value

A cache file unique to the model contents and RStan toolchain.
