# Display the specs object

Print a specs object to the console.

## Usage

``` r
print_specs(object, header = "Model specifications:\n", prefix = "  ")
```

## Arguments

- object:

  specs list to be displayed.

- header:

  line to be catted at start of printout

- prefix:

  text to prepend to the start of each line that follows the header
