# Development files

This directory contains analyses and other maintenance artifacts that support
package development but are not included in the built R package.

Standalone analyses and their rendered outputs belong under `analysis/`.
